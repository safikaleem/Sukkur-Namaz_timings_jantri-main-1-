<?xml version="1.0" encoding="utf-8"?>
<!-- Chip behind the live countdown. A tint only - the text colour itself is
     still set from PrayerWidgetProvider, so day/night accents keep working. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#141565C0" />
    <corners android:radius="9dp" />
    <stroke android:width="1dp" android:color="#1F1565C0" />
</shape>
